# Database
DATABASE_URL="postgresql://postgres:password@localhost:5432/qclue"

# QR token signing — generate with: openssl rand -hex 32
QR_SIGNING_KEY=""

# Admin session JWT signing — generate with: openssl rand -hex 32
SESSION_SECRET=""

# AI assist via OpenRouter (https://openrouter.ai)
OPENROUTER_API_KEY=""

# App URL (used in OpenRouter HTTP-Referer header)
APP_URL="http://localhost:3000"
